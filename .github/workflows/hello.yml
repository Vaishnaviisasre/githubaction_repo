name: Hello Actions
on:
  push:
    branches: 
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@4

      - name: Say Hello
        run: echo "Hello from Github action!"
